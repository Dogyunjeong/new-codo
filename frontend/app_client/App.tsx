import 'react-native-gesture-handler';
import { StatusBar } from 'expo-status-bar';

// This is the entry point but Expo Router uses app/_layout.tsx
export default function App() {
  return <StatusBar style="auto" />;
}
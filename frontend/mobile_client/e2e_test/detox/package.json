{
  "name": "ziririt-e2e-tests",
  "version": "1.0.0",
  "description": "E2E tests for Ziririt mobile app using Detox",
  "private": true,
  "scripts": {
    "test": "detox test",
    "build:ios": "detox build -c ios",
    "build:android": "detox build -c android",
    "test:ios": "detox test -c ios",
    "test:android": "detox test -c android"
  },
  "devDependencies": {
    "detox": "^20.25.0",
    "jest": "^29.7.0",
    "jest-circus": "^29.7.0"
  }
}
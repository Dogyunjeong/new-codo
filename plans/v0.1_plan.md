# V0.1 System Design: Goal Progress Social App

## Technology Stack

### Frontend (React Native)
- **Framework**: React Native 0.72+ with TypeScript
- **State Management**: Redux Toolkit + RTK Query
- **Real-time**: Socket.io-client
- **Authentication**: JWT with refresh tokens
- **Push Notifications**: @react-native-firebase/messaging

### Microservices Architecture
- **Runtime**: Node.js 18+ with TypeScript
- **Framework**: Express.js with Helmet, CORS
- **Database**: Cloud SQL PostgreSQL per service
- **Caching**: Cloud Memorystore Redis
- **Real-time**: Socket.io
- **Media Storage**: Cloud Storage + Cloud CDN
- **Authentication**: JWT + Refresh Token strategy
- **Email**: SendGrid
- **Containerization**: Docker containers

### Infrastructure (GCP + Pulumi IaC)
- **Compute**: Google Cloud Run (serverless containers)
- **Database**: Cloud SQL PostgreSQL (per service)
- **Cache**: Cloud Memorystore Redis
- **Storage**: Cloud Storage + Cloud CDN
- **Networking**: Cloud Load Balancer + VPC
- **Monitoring**: Cloud Monitoring + Cloud Logging + Sentry
- **CI/CD**: Cloud Build + GitHub Actions
- **IaC**: Pulumi for infrastructure management
- **Service Discovery**: Cloud Run service-to-service communication

## Microservices Architecture

### Service Overview

#### 1. Auth Service
**Responsibility**: User authentication, authorization, and session management
**Database**: auth_db (users, refresh_tokens, sessions)
**Key Entities**: User credentials, JWT tokens, OAuth integrations

#### 2. Profile/Goal Service  
**Responsibility**: User profiles, goal management, and goal-related data
**Database**: profile_db (users, goals, user_settings, categories)
**Key Entities**: User profiles, goals, goal categories, privacy settings

#### 3. Feed Service
**Responsibility**: Feed generation, recommendations, and content discovery
**Database**: feed_db (feed_cache, user_follows, trending_data)
**Key Entities**: Feed algorithms, user relationships, content recommendations

#### 4. Post/Inspiration Service
**Responsibility**: Content creation, social interactions, and inspiration chains
**Database**: content_db (posts, inspirations, likes, comments, media)
**Key Entities**: Posts, inspiration chains, social interactions, media files

## Service APIs & Communication

### 1. Auth Service API
```
POST   /auth/register
POST   /auth/login  
POST   /auth/refresh
POST   /auth/logout
POST   /auth/verify-token
GET    /auth/user-info/:id
```

### 2. Profile/Goal Service API
```
GET    /profiles/:id
PUT    /profiles/:id
GET    /profiles/:id/goals
POST   /goals
GET    /goals/:id
PUT    /goals/:id
DELETE /goals/:id
POST   /users/:id/follow
DELETE /users/:id/follow
GET    /users/:id/followers
GET    /users/:id/following
```

### 3. Feed Service API
```
GET    /feed/home/:userId
GET    /feed/explore
GET    /feed/trending
POST   /feed/refresh/:userId
GET    /search?q=:query&type=:type
```

### 4. Post/Inspiration Service API
```
POST   /posts
GET    /posts/:id
PUT    /posts/:id
DELETE /posts/:id
POST   /posts/:id/like
DELETE /posts/:id/like
GET    /posts/:id/comments
POST   /posts/:id/comments
POST   /posts/:id/inspire
GET    /inspirations/:id/chain
GET    /inspirations/trending
POST   /media/upload
```

### Inter-Service Communication
- **Auth Service** → Profile Service: User validation
- **Profile Service** → Feed Service: User relationships, goal data
- **Post Service** → Feed Service: New content notifications
- **Feed Service** → Post Service: Content retrieval for feeds
- **All Services** → Auth Service: Token verification

## System Architecture Design

### Data Flow Architecture

#### Client → API Gateway → Microservices
```
Mobile App → Cloud Load Balancer → Cloud Run Services
```

#### Service-to-Service Communication
- **Synchronous**: HTTP/REST for real-time operations
- **Asynchronous**: Cloud Pub/Sub for event-driven updates
- **Caching**: Cloud Memorystore Redis for performance

### Infrastructure Components

#### GCP Cloud Run Services
- **auth-service**: Authentication and user management
- **profile-service**: User profiles and goal management  
- **feed-service**: Content discovery and feed generation
- **post-service**: Content creation and social interactions

#### Shared Infrastructure
- **Cloud SQL**: PostgreSQL databases per service
- **Cloud Storage**: Media file storage with CDN
- **Cloud Memorystore**: Redis for caching and sessions
- **Cloud Load Balancer**: API gateway and traffic routing
- **Cloud Pub/Sub**: Event-driven communication

## Microservice Architecture (Based on Existing Boilerplate)

### Service Structure Pattern
Each microservice follows the existing boilerplate pattern:
```
service-name/
├── src/
│   ├── api/
│   │   └── feature/
│   │       ├── Feature.handler.mts    # Request handling logic
│   │       ├── Feature.routes.mts     # Route definitions
│   │       └── Feature.service.mts    # Business logic
│   ├── configs/
│   │   └── app.config.mts            # Service configuration
│   └── index.mts                     # Service entry point
├── package.json                      # Dependencies & scripts
└── ecosystem.config.cjs             # PM2 configuration
```

### 1. Auth Service
**Base**: `auth-service` (port 4101)
**Dependencies**: `base-server` package
**API Structure:**
```
src/api/
├── auth/
│   ├── Auth.handler.mts         # Login, register, refresh
│   ├── Auth.routes.mts          # Authentication routes
│   └── Auth.service.mts         # JWT & credential logic
└── verification/
    ├── Verification.handler.mts  # Token verification
    ├── Verification.routes.mts   # Verification routes
    └── Verification.service.mts  # Token validation logic
```

### 2. Profile Service
**Base**: `profile-service` (port 4102)
**Dependencies**: `base-server` package
**API Structure:**
```
src/api/
├── profiles/
│   ├── Profile.handler.mts      # Profile CRUD operations
│   ├── Profile.routes.mts       # Profile routes
│   └── Profile.service.mts      # Profile business logic
├── goals/
│   ├── Goal.handler.mts         # Goal management
│   ├── Goal.routes.mts          # Goal routes
│   └── Goal.service.mts         # Goal business logic
└── relationships/
    ├── Relationship.handler.mts  # Follow/unfollow
    ├── Relationship.routes.mts   # Social routes
    └── Relationship.service.mts  # Relationship logic
```

### 3. Feed Service
**Base**: `feed-service` (port 4103)
**Dependencies**: `base-server` package
**API Structure:**
```
src/api/
├── feed/
│   ├── Feed.handler.mts         # Feed generation
│   ├── Feed.routes.mts          # Feed routes
│   └── Feed.service.mts         # Feed algorithms
├── discover/
│   ├── Discover.handler.mts     # Content discovery
│   ├── Discover.routes.mts      # Discovery routes
│   └── Discover.service.mts     # Discovery logic
└── search/
    ├── Search.handler.mts       # Search functionality
    ├── Search.routes.mts        # Search routes
    └── Search.service.mts       # Search algorithms
```

### 4. Post Service
**Base**: `post-service` (port 4104)
**Dependencies**: `base-server` package
**API Structure:**
```
src/api/
├── posts/
│   ├── Post.handler.mts         # Post CRUD operations
│   ├── Post.routes.mts          # Post routes
│   └── Post.service.mts         # Post business logic
├── interactions/
│   ├── Interaction.handler.mts  # Likes, comments
│   ├── Interaction.routes.mts   # Interaction routes
│   └── Interaction.service.mts  # Social interaction logic
├── inspirations/
│   ├── Inspiration.handler.mts  # Inspiration chains
│   ├── Inspiration.routes.mts   # Inspiration routes
│   └── Inspiration.service.mts  # Chain logic
└── media/
    ├── Media.handler.mts        # Media upload/processing
    ├── Media.routes.mts         # Media routes
    └── Media.service.mts        # Media handling logic
```

## Implementation Phases

### Phase 1: Infrastructure & Auth Service (Weeks 1-3)
**Infrastructure:**
- Setup Pulumi IaC for GCP Cloud Run
- Configure Cloud SQL databases per service
- Setup Cloud Load Balancer and networking
- Configure Cloud Storage and CDN

**Auth Service:**
- Authentication and JWT token management
- User registration and login endpoints
- Token verification service for other microservices

**Goals:**
- Complete infrastructure foundation
- Working authentication service
- Service-to-service communication established

### Phase 2: Profile/Goal Service (Weeks 4-6)
**Profile Service:**
- User profile management
- Goal creation and management
- User relationship system (follows)
- Privacy settings and permissions

**Goals:**
- Complete user and goal management
- Social relationship foundation
- Service integration with auth

### Phase 3: Post/Inspiration Service (Weeks 7-10)
**Post Service:**
- Post creation and management
- Media upload to Cloud Storage
- Social interactions (likes, comments)
- Basic inspiration feature (no chains yet)

**Goals:**
- Content creation functionality
- Media handling working
- Basic social interactions

### Phase 4: Feed Service (Weeks 11-13)
**Feed Service:**
- Home feed generation
- Content discovery algorithms
- Search functionality
- Feed caching with Redis

**Goals:**
- Personalized feed working
- Content discovery features
- Performance-optimized feeds

### Phase 5: Inspiration Chains & Optimization (Weeks 14-16)
**Advanced Features:**
- Inspiration chain creation and traversal
- Chain depth tracking and visualization
- Trending chains algorithm
- Performance optimization across all services

**Goals:**
- Complete inspiration chain functionality
- System-wide performance optimization
- Production-ready microservices

## Pulumi Infrastructure as Code

### GCP Resource Architecture
```typescript
// Infrastructure components managed by Pulumi
- Cloud Run services (4 microservices)
- Cloud SQL PostgreSQL instances (per service)
- Cloud Load Balancer with SSL termination
- Cloud Storage buckets for media
- Cloud CDN for global content delivery
- Cloud Memorystore Redis for caching
- VPC networks and firewall rules
- Cloud IAM roles and service accounts
```

### Service Deployment Strategy
- **Blue-Green Deployment**: Zero-downtime deployments
- **Auto-scaling**: Cloud Run automatic scaling based on traffic
- **Health Checks**: Service health monitoring and auto-recovery
- **Load Balancing**: Traffic distribution across service instances

### CI/CD with Cloud Build
```yaml
# cloudbuild.yaml
steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/auth-service', './auth-service']
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/auth-service']
  - name: 'gcr.io/cloud-builders/gcloud'
    args: ['run', 'deploy', 'auth-service', '--image', 'gcr.io/$PROJECT_ID/auth-service']
```

## Security Architecture

### Service-to-Service Security
- **JWT Tokens**: Service authentication with short-lived tokens
- **Network Security**: VPC private networking between services
- **IAM Roles**: Principle of least privilege for service accounts
- **API Rate Limiting**: Per-service request throttling
- **HTTPS Only**: TLS encryption for all communications

### Data Protection
- **Database Security**: Cloud SQL with encrypted connections
- **Input Validation**: Request validation at API gateway level
- **Secret Management**: Google Secret Manager for credentials
- **Access Controls**: Role-based access control (RBAC)
- **Audit Logging**: Cloud Audit Logs for security monitoring

## Scalability & Performance

### Microservices Scaling
- **Auto-scaling**: Cloud Run automatic scaling based on CPU/memory
- **Load Distribution**: Cloud Load Balancer across service instances
- **Database Scaling**: Cloud SQL read replicas for read-heavy operations
- **Caching Strategy**: Redis for frequently accessed data (feeds, user sessions)

### Performance Optimization
- **CDN**: Cloud CDN for global media delivery
- **Database Indexing**: Optimized indexes for common query patterns
- **Connection Pooling**: Efficient database connection management
- **Async Processing**: Cloud Pub/Sub for non-blocking operations

## Monitoring & Observability

### GCP Native Monitoring
- **Cloud Monitoring**: Service metrics and alerting
- **Cloud Logging**: Centralized log aggregation
- **Cloud Trace**: Distributed tracing across microservices
- **Cloud Profiler**: Performance profiling for optimization

### Custom Metrics
- **Business Metrics**: User engagement, goal creation rates
- **Technical Metrics**: API latency, error rates, throughput
- **Service Health**: Health checks and uptime monitoring

## System Design Goals

### Technical Requirements
- **Scalability**: Handle 100K+ concurrent users
- **Availability**: 99.9% uptime target
- **Performance**: API response time < 200ms (95th percentile)
- **Reliability**: Fault tolerance and graceful degradation

### Operational Excellence
- **Automated Deployment**: Pulumi IaC + Cloud Build CI/CD
- **Monitoring**: Comprehensive observability across all services
- **Security**: Zero-trust security model with service mesh
- **Cost Optimization**: Auto-scaling and resource optimization

## Success Metrics

### Technical KPIs
- Service availability > 99.9%
- API response time < 200ms (P95)
- Auto-scaling efficiency (cost vs. performance)
- Zero-downtime deployments

### Business KPIs
- User registration completion rate > 80%
- Goal creation rate > 60% of users
- Daily active user engagement > 30%
- Inspiration chain creation rate > 15%

This system design provides a scalable, secure, and maintainable microservices architecture using GCP Cloud Run and Pulumi IaC for a goal progress social application.